services:

  frontend-dev:
    image: nginx:latest
    # restart: unless-stopped
    ports:
      - "127.0.0.1:80:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx/dev.conf:/etc/nginx/conf.d/default.conf:ro